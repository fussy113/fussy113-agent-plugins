# 指示

やり取りは日本語で行うこと。

## スキル執筆の方針

`plugins/*/skills/*/SKILL.md` を追加・改訂するときは次に揃える。

- **出力は結論先出し**。判定・結果を先頭に置き、詳細を後ろに回す。該当しないセクションは省略可にし、「なし」の羅列で埋めさせない。
- **長さは内容に見合わせる**。埋め草・定型文・既に書いたことの言い換えを足さない。ディスクに書き出すドキュメント(仕様書・メモリ・レポート)には長さ校正の一文を入れる。
- **モデル自身の再確認を指示しない**。「必ず二重チェック」「再確認してから答える」等は自己修正と二重に走りコストだけ増える。一方、**外部で実際に走る検証**(テスト・リンタ・`mise install` 等のコマンド実行)は明示的に指示する。この2つを混同しない。
- **サブエージェント委譲は独立した大物に限る**。数回のツール呼び出しで終わる作業は委譲しない。自分の作業の検証には使わない。起動数は低く保つ。
- **狭いタスクのスキルはスコープを固定する**。「作業範囲は○○だけ」を明記し、気づいた改善点は提示までに留めて適用は別スキルに渡す。
- **長い工程のスキルにはナレーションのケイデンスを書く**。最初に1文、途中は重要な発見・方針転換のみ、最後は結論から。
- **レビュー系スキルで報告を絞らせない**。「重大なものだけ」は文字どおり受け取られ報告が減る。全部挙げさせ、重要度の切り分けは別セクション・別パスで行う。
- **effort と出力長は別のつまみ**。effort を下げても可視応答は短くならない。短くしたいなら簡潔さを明示的に指示する。
- **共有スキルは宿主中立に書く**。`plugins/` は Claude Code と Codex の両方から読まれる。両対応プラグインの SKILL.md には特定宿主のコマンド(`/effort` 等)・パス(`.claude/`)・MCP 識別子を素で書かず、必要なら「Claude Code の場合」と条件付きにする。プラグインを両対応にするときは `.codex-plugin/plugin.json` と `.agents/plugins/marketplace.json` のエントリを両方追加する。
- **同じ規則を二度書かない**。「注意事項」で本文を再掲しない。再掲したくなったら本文側に一本化する。
- **矛盾する規則を残さない**。相反する指示は欠落より高くつく(両立を試みて浪費する)。
- **不要な絶対語を使わない**。「必ず」「常に」は鉄則(真の不変条件)にだけ使い、手順の強調には使わない。
- **経路より完了条件**。中間手順を1行ずつ規定せず、「何が満たされたら完了か」「何を証跡として残すか」「どこから先はユーザー承認か」を書く。
- **否定より肯定例**。「〜するな」より、望ましい振る舞いの例を示す。
